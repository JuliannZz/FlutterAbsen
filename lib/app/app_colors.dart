import 'package:flutter/material.dart';

class AppColors {
  static final ColorScheme colorScheme = ColorScheme.fromSeed(seedColor: AppColors.pink);

  static const pink= Colors.pinkAccent;
  static const hitam= Color(0xFF0f111a);
}